"""TODOs for the guided SDK build.

Implement these during the workshop:

1. Create a Copilot SDK session.
2. Load agents/triage.agent.md as a custom agent.
3. Register skills/ as the skill directory.
4. Send the AlertEvidenceBundle to the triage agent.
5. Send the draft to the challenger.
6. Render Markdown and JSON artifacts.

Keep API writes and shell execution human-approved.
"""
